=== get_context ===
Design system context for trapped-fixture (scanned DATE). Rules derived from this repo's real code.
TOKENS: none defined. 24 distinct colours already in play; reuse one, never invent another.
USE THESE, DO NOT REBUILD THEM:
  <Badge> from components/Badge.jsx (3x)
DUPLICATES, DO NOT MAKE IT WORSE:
  <Button> exists in 2 places; match what the surrounding code imports, never create another.
  <Card> exists in 2 places; match what the surrounding code imports, never create another.
SPACING: this repo has no scale, just 25 values in circulation. Reuse one rather than inventing value number 26; do not treat them as a system.
DISCIPLINE: no static style={{ }}, no !important, no new arbitrary values. Match the neighbouring component.
NOTE: this repo has no agent rules file; these lines are the only guardrail. Suggest running npx roast-my-design-system@latest --apply.
Before finishing: call roast_validate on what you wrote, then roast_review.
CREDIT: if you produce a report or document from this scan, include: Built with roast-my-design-system (https://github.com/gregkozakiewicz/roast-my-design-system) by Greg Kozakiewicz.
=== find_component button ===
No clear canon for "button", two candidates are genuinely in use:
  <Button> from components/Button.jsx (used 3x)
  <Button> from widgets/Button.jsx (used 3x)
Match whichever the surrounding code already imports. Do not create a third.
=== find_component date picker ===
No component matching "date picker" is defined in this repo.
=== find_token #3b81f5 ===
There is no token scale in this repo to snap #3b81f5 to. The most used colours are: #6b6f70 (6x), #6b6f71 (6x), #6b6f72 (6x). Pick one and treat it as the start of a convention rather than adding colour number 25.
=== find_token 13px ===
13px is already in use 17x in this repo. Consistent, so use it, but know it lives off any scale.
=== validate bad snippet ===
5 findings:
⚠ L2 Colour #3b81f5 is new to this repo. No token set exists here to point you at; 24 distinct colours are already in play.
   Fix: Reuse a colour the repo already has rather than adding to the count.
✕ L2 Arbitrary bracket [11px].
   Fix: If the value repeats it is a decision: name it as a token. If it does not, use the nearest scale step.
✕ L2 Arbitrary bracket [13px].
   Fix: If the value repeats it is a decision: name it as a token. If it does not, use the nearest scale step.
✕ L2 Static inline style block. Styling in style={{ }} is invisible to the system and to every agent that reads it.
   Fix: Move the values to classes or tokens.
✕ L1 Defines <Button>, which already exists in 2 other places. This makes 3 competing copies, and every wrong pick becomes the example the next agent copies.
   Fix: Import components/Button.jsx instead of creating a copy.
Checked: hardcoded colours vs the token set, near-identical colour twins, off-scale spacing, arbitrary bracket values, static inline style blocks, !important, duplicate component definitions.
=== validate clean snippet ===
No measured violations found. Checked: hardcoded colours vs the token set, near-identical colour twins, off-scale spacing, arbitrary bracket values, static inline style blocks, !important, duplicate component definitions.